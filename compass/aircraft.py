# selects aircraft at random from a .toml file.

import random

def select_aircraft(pilots, occupants, dep_airport, arr_airport)

viable_aircraft{''}    #dictionary of viable aircraft
aircraft_filename = raw_input("Path of aircraft toml file to use: ")

# For each variation in filename

	# If aircraft_pilots <= pilots and seats >= occupants
	# and range < distance(dep_airport, arr_airport)




